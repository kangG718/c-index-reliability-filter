# Main experiment

C_Index(step2,_sharpe_logic_changed).ipynb is the current main experiment candidate. It uses STEP=2 and the corrected Sharpe calculation.

The notebook is preserved without changing its experimental logic or recorded outputs.
